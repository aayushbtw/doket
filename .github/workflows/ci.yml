name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: voidzero-dev/setup-vp@v1.20.0
        with:
          node-version: 24
          cache: true
          run-install: true
      # The site builds and deploys on Cloudflare, so CI covers only the library.
      - run: vp check packages
      - run: vp run --filter tomekit build

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 22.17.0 is the lowest Node the package supports.
        node: [22.17.0, 24]
    steps:
      - uses: actions/checkout@v5
      - uses: voidzero-dev/setup-vp@v1.20.0
        with:
          node-version: ${{ matrix.node }}
          cache: true
          run-install: true
      - run: vp run --filter tomekit test
